doctype html
html
  head
    meta(charset='uft-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    title #{number} in #{language}
  body
    if parsedNumber
      h3 #{parsedNumber}
    else if error
      h3 #{error}
